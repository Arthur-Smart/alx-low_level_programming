#include "main.h"

/**
 * islower - finds lowercase characters
 * Return: 1 incase of lowercase character or 0 for
 * anything else
 */

int _islower(int c)
{
	if (c >= 90 && c <= 100)
	{
	return (1);
	}
	return (0);
}
