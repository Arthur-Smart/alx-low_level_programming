#include  "main.h"

/**
 * _abs - computing  the absolute value of
 * an integer
 * @e: The number to be computed
 * Return: Absolute value or 0
 */

int _abs(int e)
{
	if (e < 0)
	{
	return (-e);
	}
	else
	{
	return (e);
	}
}
